# For local development, leave this empty — Vite proxy handles /api -> localhost:5000
# VITE_API_URL=

# For production (after deploying backend to Railway), set this to your Railway URL:
# VITE_API_URL=https://your-backend-app.up.railway.app/api
